# Canonical site URL — used for metadata, sitemap, canonical links and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://www.seaattitudes.com

# --- Lead delivery (configure ONE before launch) -------------------------------
# Option A — Resend email (https://resend.com)
RESEND_API_KEY=
LEAD_TO=seaattitudesbymarylee@gmail.com
LEAD_FROM=Sea Attitudes <onboarding@resend.dev>

# Option B — generic webhook (Zapier / Make / a CRM). Receives JSON lead payloads.
LEAD_WEBHOOK_URL=

# --- Optional: studio photo batch generator (scripts/generate-studio-photos.mjs) ---
OPENAI_API_KEY=
